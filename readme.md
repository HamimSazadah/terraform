# Terraform Commands

```
terraform init -> prepare provider
terraform apply -> apply/run provisioning services
terraform destroy -> destroy service
terraform output -> show output
terraform show -> show current state
terraform state list -> list resource
terraform validate -> validate terraform config
```